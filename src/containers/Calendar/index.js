import React from 'react';
import {Text, View} from 'react-native';

//Styles
import styles from '../commonStyles';

const {screenWrapper} = styles;

export default CalendarScreen = () => (
  <View style={screenWrapper}>
    <Text>Calendar. Nothing here</Text>
  </View>
);
